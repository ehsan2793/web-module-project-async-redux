const initialstate = {
  isLoading: false,
  missionsData: [],
  error: '',
};

const missonReducer = (state = initialstate, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default missonReducer;
